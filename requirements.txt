# ============================================================
# noise-xorcist 项目依赖
# ============================================================
# 安装方式: pip install -r requirements.txt
#
# 注意: DeepFilterNet 相关模型 (df1/df2/df3) 需要额外安装
# libdf 和 df 包，请参考 https://github.com/Rikorose/DeepFilterNet
# ============================================================

# ---------- 核心依赖 ----------
torch>=1.11.0
numpy>=1.24.0
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.10.0
tqdm>=4.64.0
omegaconf>=2.3.0

# ---------- 深度学习 ----------
einops>=0.7.0

# ---------- 语音增强评估 ----------
pesq>=0.0.4
pystoi>=0.3.3

# ---------- 并行计算 ----------
joblib>=1.2.0

# ---------- 数据处理 ----------
pandas>=2.0.0

# ---------- 类型标注 ----------
typing_extensions>=4.0.0

# ---------- 可选依赖 ----------
# loguru>=0.7.0          # DeepFilterNet 模型需要
# p_tqdm>=1.0.0          # 并行进度条（evaluation 使用）
# espnet2                 # DNSMOS 评估需要
# libdf                   # DeepFilterNet C 库（需从源码编译）
# ptflops>=0.7           # 模型 FLOPs 统计（调试用）
