test_dataset: 
  noisy_dir: /input0/noisy_testset_wav
  clean_dir: /input0/clean_testset_wav

network:
  exp_path: /hyperai/home/noise-xorcist/runs/exp_gtcrn_2026-08-23-01h26m
  config: ${network.exp_path}/config.yaml
  ckpt_name: best_model_180
  checkpoint: ${network.exp_path}/checkpoints/${network.ckpt_name}.tar
  enh_folder: ${network.exp_path}/${network.ckpt_name}/enhanced
